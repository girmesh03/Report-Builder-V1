/**
 * Prompt version constants.
 *
 * @module utils/promptVersions
 */

/** @type {{V1: string, V2: string}} */
const PROMPT_VERSIONS = Object.freeze({
  V1: 'v1.1.0',
  V2: 'v2.0.0',
});

export default PROMPT_VERSIONS;
