/**
 * Route path constants.
 *
 * @module utils/routePaths
 */

export const ROUTE_PATHS = Object.freeze({
  HOME: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  OAUTH_CALLBACK: '/oauth/callback',
  DASHBOARD: '/dashboard',
  REPORTS: '/reports',
  PROFILE: '/profile',
});
